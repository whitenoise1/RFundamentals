# Data cache (large, regenerated by build)
cache/

# Planning and setup docs (internal only)
CLAUDE.md
CLAUDE_CODE_SETUP.md
WORKPLAN.md
RESEARCH_INDICATORS.md
docs/WORKPLAN.md
docs/RESEARCH_INDICATORS.md
RFundamentals_setup.zip

# Reference code (read-only, not a dependency)
ref/

# Superseded source copies
sources/

# Superseded scripts
load_snapshot_example.R
run_snapshot.R

# Google Drive sync conflict copies (RF-3): Drive renames a file to
# "foo (1).ext" on a sync conflict. Never track these; loaders also skip
# them at runtime via .drop_drive_conflict_paths().
* ([0-9]).*
* ([0-9][0-9]).*
* ([0-9][0-9][0-9]).*

# R / OS artifacts
.Rhistory
.RData
.Rproj.user
.DS_Store

# Claude Code local config
.claude/

# r_F fundamental regime feature module (private, unpublished)
# Note: cache/rF/ and cache/prices/SPY_* are already covered by cache/ above.
R/feature_compute_rF.R
run_rF_features.R
demo_rF.R
tests/test_feature_compute_rF.R
tests/test_feature_compute_rF_integration.R
tools/diag_rF_smoke.R
tools/gate_test_rF_shape.R
tools/visualize_rF_outputs.R
docs/PLAN_rF_MODULE.md
docs/RF_MODULE_TECHNICAL_REFERENCE.md
